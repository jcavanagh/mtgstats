/* global Ember */
if (typeof define !== 'function') { var define = require('amdefine')(module); }

/**
 * Charts controller
 * 
 * @class
 * @author Joe Cavanagh
 */
define([], function() {
    return Ember.Controller.extend({
        //Nothing to do
    });
});
